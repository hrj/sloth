name: Build Extension

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Zip Extension
        run: zip -r sloth.zip html js img manifest.json

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sloth-extension
          path: sloth.zip
